{
  "name": "he8um-profile-dashboard",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "generate": "node scripts/generate-profile.mjs",
    "refresh": "node scripts/generate-profile.mjs --refresh",
    "validate": "node scripts/validate-profile.mjs",
    "test": "node --test"
  }
}
